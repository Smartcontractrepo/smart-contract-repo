require("@nomiclabs/hardhat-ethers");
require("dotenv").config();

module.exports = {
  solidity: "0.8.9",
  networks: {
    hardhat: {}
    // add testnet/mainnet entries here using process.env keys
  }
};+
