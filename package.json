{
  "name": "smart-contract-repo",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@openzeppelin/contracts": "^4.8.0",
    "chai": "^4.3.0",
    "ethers": "^5.7.0",
    "hardhat": "^2.17.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  }
}+
